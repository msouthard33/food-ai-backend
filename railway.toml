[build]
builder = "dockerfile"
dockerfile = "./Dockerfile"

[deploy]
startCommand = "sh -c 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'"
restartPolicy = "always"
healthcheck = { path = "/ready", interval = 10 }
